import React from 'react';
import { LiveScreen } from '../screens/LiveScreen';

export default {
  title: 'Screens/Live',
  component: LiveScreen
};

export const Open = () => <LiveScreen />;
export const Revealed = () => <LiveScreen />;
export const Stats = () => <LiveScreen />;
